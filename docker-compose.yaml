version: '3.9'
services:
  lawsuit-metadata-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lawsuit-metadata-api
    environment:
      - NODE_ENV=production
      - PORT=8282
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    command: node dist/main
    ports:
      - '8282:8282'
    restart: always
    networks:
      - traefik-net
    labels:
      - 'traefik.enable=true'
      - 'traefik.http.routers.metadataapi.rule=Host(`metadata.api.analisesprosolutti.com`)'
      - 'traefik.http.routers.metadataapi.entrypoints=websecure'
      - 'traefik.http.routers.metadataapi.tls.certresolver=mytlschallenge'
      - 'traefik.http.services.metadataapi.loadbalancer.server.port=8282'
    expose:
      - '8282'
networks:
  traefik-net:
    external: true
